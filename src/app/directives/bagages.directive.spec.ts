import { BagagesDirective } from './bagages.directive';

describe('BagagesDirective', () => {
  it('should create an instance', () => {
    const directive = new BagagesDirective();
    expect(directive).toBeTruthy();
  });
});
