import { VolClasseDirective } from './vol-classe.directive';

describe('VolClasseDirective', () => {
  it('should create an instance', () => {
    const directive = new VolClasseDirective();
    expect(directive).toBeTruthy();
  });
});
